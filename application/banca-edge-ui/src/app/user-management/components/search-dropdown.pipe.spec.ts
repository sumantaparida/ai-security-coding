import { SearchDropdownPipe } from './search-dropdown.pipe';

describe('SearchDropdownPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDropdownPipe();
    expect(pipe).toBeTruthy();
  });
});
