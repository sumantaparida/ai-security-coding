import { DisplaySectionPipe } from './display-section.pipe';

describe('DisplaySectionPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplaySectionPipe();
    expect(pipe).toBeTruthy();
  });
});
