import { CheckRolesPipe } from './check-roles.pipe';

describe('ChecRolesPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckRolesPipe();
    expect(pipe).toBeTruthy();
  });
});
