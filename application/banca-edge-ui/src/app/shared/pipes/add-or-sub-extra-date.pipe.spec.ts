import { AddOrSubExtraDatePipe } from './add-or-sub-extra-date.pipe';

describe('AddOrSubExtraDatePipe', () => {
  it('create an instance', () => {
    const pipe = new AddOrSubExtraDatePipe();
    expect(pipe).toBeTruthy();
  });
});
